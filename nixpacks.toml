[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "PLAYWRIGHT_BROWSERS_PATH=/app/pw-browsers playwright install chromium --with-deps",
]

[start]
cmd = "PLAYWRIGHT_BROWSERS_PATH=/app/pw-browsers uvicorn server:app --host 0.0.0.0 --port $PORT"